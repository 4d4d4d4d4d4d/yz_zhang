build/
obj_dir/
*.vcd
*.fst
__pycache__/
*.pyc
# Test programs are regenerated by tools/gen_*.py from fixed seeds, so they
# are build output, not source. prog.txt is the one hand-written smoke test.
tests/vectors/*
!tests/vectors/prog.txt
